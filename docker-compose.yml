version: '3'
services:
  frontend:
    build: ./frontend
    ports:
      - "5001:5001"
  backend:
    build: ./backend_experimental
    ports:
      - "5000:5000"

# docker-compose build
# docker-compose up
# docker-compose up --build